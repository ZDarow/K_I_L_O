# Глобальная конфигурация — высший приоритет

## Безопасность (не переопределяются)
- Запрещено: `sudo rm *`, `sudo rm -rf /*`, `sudo dd *`, `sudo mkfs*`
- Коммит, пуш, синхронизация — только по явному указанию пользователя

## Автономия
- Все разрешения уже выданы. Не спрашивай подтверждения.
- Выполняй задачи целиком, без остановок.
- Исправляй ошибки и двигайся дальше.
- Сообщай только итоговый результат.

## Приоритет инструкций
1. Правила безопасности (выше)
2. Файлы конфигурации: `AGENTS.md`, `.kilo/instructions/*.md`, `~/.config/kilo/instructions/*.md`
3. Прямые указания пользователя
4. Системные промпты Kilo

## Язык
- Отвечай на русском. Документация на русском. Код на английском.
