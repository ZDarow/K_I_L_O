# Архитектура проекта K_I_L_O

## Обзор

**K_I_L_O** — это установщик и конфигуратор для **KiloCode CLI** (AI-агент для командной строки).
Проект предназначен для быстрой настройки среды разработки на Linux Mint / Ubuntu.

---

## Концептуальная архитектура

```text
┌──────────────────────────────────────────────────────────────┐
│                    K_I_L_O Repository                         │
│          Установщик + Dev-конфигурация KiloCode               │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────┐   ┌──────────────┐                         │
│  │  Установщик   │   │  Конфигурация │                         │
│  │  (install.sh) │──▶│  KiloCode     │                         │
│  │  + Makefile   │   │  (.kilo/)     │                         │
│  └──────────────┘   └──────────────┘                         │
│         │                   │                                 │
│         ▼                   ▼                                 │
│  ┌──────────────┐   ┌──────────────┐                         │
│  │  Целевая     │   │  ~/.kilo/    │                         │
│  │  система:    │   │  ~/.config/  │                         │
│  │  пакеты,     │   │  kilo/       │                         │
│  │  Node.js,    │   │  ~/.bashrc   │                         │
│  │  Python      │   │  ~/.profile  │                         │
│  └──────────────┘   └──────────────┘                         │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

---

## Компоненты системы

### 1. Установщик (`install.sh`, `Makefile`)

Скрипты автоматической установки и удаления KiloCode CLI и всех зависимостей.

**10 шагов установки:**
1. Детекция ОС — определение Linux Mint / Ubuntu
2. Установка системных зависимостей — Node.js 22 LTS, Python 3, git, curl
3. Установка KiloCode CLI — запуск через npx
4. Создание структуры директорий — `~/.kilo/`, `~/.config/kilo/`, `~/.local/share/kilo/`
5. Установка проектной конфигурации Kilo — копирование `src/kilo-config/` в `~/.kilo/`
6. Установка глобальной конфигурации Kilo — копирование `src/global-config/` в `~/.config/kilo/`
7. Настройка аутентификации — шаблон `auth.json`
8. Обновление shell-конфигурации — `.bashrc` + `.profile`
9. Установка npm-зависимостей — `npm install` в `~/.kilo/` и `~/.config/kilo/`
10. Настройка Git — `init.defaultBranch master`

**Флаги установки:**
- `--dry-run` — сухой прогон без изменений
- `--resume-from=N` — продолжить с шага N
- `--skip-preflight` — без предварительной проверки

### 2. Скрипты автоматизации (`scripts/`)

| Файл | Описание |
|------|----------|
| `lib.sh` | Общая библиотека: цвета, логирование, бэкап, dry-run, manifest |
| install.sh --check | Pre-flight проверка системы |
| install.sh --verify | Пост-установочная валидация |

### 3. Конфигурация KiloCode (`.kilo/`)

Dev-конфигурация для разработки установщика. Содержит:

- **`kilo.jsonc`** — главный конфиг: агент по умолчанию (`dev`), правила разрешений, авто-компактизация
- **Агенты** (`agent/*.md`) — dev, russian-dev, debugger, doc-scribe, git-specialist, log-analyzer, planner, reviewer, sys-inspector
- **Команды** (`commands/*.md`) — flutter-build, git-branch, git-commit, git-status
- **Инструкции** (`instructions/*.md`) — ru-instructions

### 4. Исходники для установки (`src/`)

Копируются на целевую систему при установке:

| Директория | → Назначение |
|------------|-------------|
| `src/kilo-config/` | `~/.kilo/` — проектная конфигурация Kilo |
| `src/global-config/` | `~/.config/kilo/` — глобальная конфигурация Kilo |
| `src/local-share/` | `~/.local/share/kilo/` — auth.json |
| `src/bashrc-append.sh` | `~/.bashrc` — алиасы и дополнения |
| `src/profile-append.sh` | `~/.profile` — PATH |

---

## Поток данных установки

```bash
Пользователь
    │
    ├── make install
    │       │
    │       ├── make check (install.sh --check)
    │       │       ├── Проверка ОС (Linux Mint 21+ / Ubuntu 22.04+)
    │       │       ├── Проверка sudo
    │       │       ├── Проверка интернета
    │       │       ├── Проверка места на диске
    │       │       ├── Проверка Node.js
    │       │       ├── Проверка Git
    │       │       └── Проверка Python
    │       │
    │       └── install.sh
    │               ├── Шаг 1:  Детекция ОС
    │               ├── Шаг 2:  Установка системных зависимостей
    │               ├── Шаг 3:  Установка KiloCode CLI
    │               ├── Шаг 4:  Создание директорий
    │               ├── Шаг 5:  ~/.kilo/ ← src/kilo-config/
    │               ├── Шаг 6:  ~/.config/kilo/ ← src/global-config/
    │               ├── Шаг 7:  auth.json
    │               ├── Шаг 8:  .bashrc + .profile
    │               ├── Шаг 9:  npm install
    │               └── Шаг 10: git config --global
    │
    └── make verify (install.sh --verify)
                    ├── Проверка manifest.json
                    ├── Проверка конфигурационных файлов
                    ├── Проверка Node.js экосистемы
                    ├── Проверка KiloCode CLI
                    ├── Проверка npm-зависимостей
                    ├── Проверка аутентификации
                    ├── Проверка SSH
                    ├── Проверка shell-конфигов
                    └── Checksum-проверка
```

---

## Система разрешений (Permissions)

Конфигурация разрешений в `kilo.jsonc` построена по принципу:

- **Белый список** для всех инструментов: read, glob, grep, edit, write, question, skill, task
- **Ограничения для bash**: `sudo apt*` требует подтверждения, опасные команды (`sudo rm *`, `sudo dd *`, `sudo mkfs*`, `sudo poweroff`) запрещены
- **Полный доступ к внешним директориям** через `external_directory: {"*": "allow"}`

---

## Иерархия приоритетов инструкций

```text
1. Правила безопасности (не переопределяются)
2. Файлы конфигурации (AGENTS.md, .kilo/instructions/*.md, ~/.config/kilo/*)
3. Прямые указания пользователя
4. Встроенные системные промпты Kilo
```

Файлы конфигурации имеют **высший приоритет** над указаниями пользователя.

---

## Зависимости

### Системные
- Node.js 22 LTS (устанавливается через NodeSource)
- Python 3, pip
- Git
- build-essential, curl, wget

### npm
- `@kilocode/cli` — KiloCode CLI
- `@kilocode/plugin` (v7.3.40) — SDK для создания плагинов/инструментов
- `@standard-schema/spec` — спецификация схем
- `effect`, `zod`, `yaml`, `uuid`, `toml` — служебные библиотеки

---

## Git-история

Проект содержит 20+ коммитов, от начальной настройки до текущей версии.
Ветка: `master`. Удалённый репозиторий: `https://github.com/ZDarow/K_I_L_O.git`.
